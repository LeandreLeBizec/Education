public class EXO3 {
/*
Q9)
On place successivement les fils d'un neud dans le sous arbre gauche et les frère d'un noeud dans son sous arbre droit


Q10)
public ArbreBinaire restructure(ArbreBinaire source){
    source.posiRacine();
    while(source.aFilsG()){
        source.posiFilsG();
    }
    if(source.aFilsD()){
        source.getFilsD().modifPere(null);
        source.modifArbreG(restructure(source.getArbreD()));
        source.modifFilsD(null);
    }
    while(source.aPere()){
        if(source.aFrere()){
            source.getFrere.modifPere(null);
            source.modifArbreD(restructure(source.getFrere());
            source.modifFrere(null);
        }
        source.posiPere();
    }
    return source;
}
*/


}
